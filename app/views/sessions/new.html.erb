<h1 class='text-center'>Вход</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(url: login_path, scope: :session) do |form| %>

      <%= form.label :email, 'Почта' %>
      <p><%= form.email_field :email, class: 'form-control' %></p>

      <%= form.label :password, 'Пароль' %>
      <p><%= form.password_field :password, class: 'form-control' %></p>

      <p><%= form.submit "Войти", class: "btn btn-primary" %></p>
    <% end %>
      <br>
      <p>Нет аккаунта? <%= link_to 'Зарегистрироваться', new_user_path %></p>

  </div>
</div>
